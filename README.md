# Service Worker for IPFS

This example shows how to use a service worker to intercept requests to an IPFS gateway and serve them from a local gateway (if available).


## Running

```bash
npx http-server -p 5000 .
```